## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `dotnet build -c Release` succeeds for `net8.0` and `net9.0`
- [ ] `dotnet test -c Release` succeeds (Docker running for integration tests)
- [ ] Public API / behavior changes noted in `CHANGELOG.md` under `[Unreleased]`
- [ ] No secrets or credentials committed
